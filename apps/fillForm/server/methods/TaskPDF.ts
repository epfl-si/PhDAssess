/**
 * From a base64, save it to the server and get back the path
 * @param pdfName
 * @param pdfAsBase64String
 * @return path where the file has been saved
 */
export const processPdf = (
  pdfName: string,
  pdfAsBase64String: string
) => {
  // 1. Build correct full GED path

  // 2. Send it to GED

  // 3. Manage timeout and errors



  return ''
}


const buildGEDPath = (pdfName: string) => {

}
